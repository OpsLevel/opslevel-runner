## [September 01, 2026](https://github.com/OpsLevel/opslevel-runner/compare/v2026.7.8...v2026.9.1)
### Feature
* Add configurable ephemeral-storage requests and limits for job pods via `job-pod-requests-ephemeral-storage` (default 64 MB) and `job-pod-limits-ephemeral-storage` (default 26624 MB) flags